#!/usr/bin/env sh

set -x #echo on

PRIVATE_KEY={{ obj.config.privateKey }}
DATA_DIR={{ obj.dbDir }}

ganache-cli --hardfork istanbul --blockTime 1 --db $DATA_DIR --account $PRIVATE_KEY,1000000000000000000000 --gasLimit 8000000 --gasPrice 0 -p 9545